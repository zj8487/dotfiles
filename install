#!/bin/bash
set -e

# create ~/.bashrc as symbolic link to our bashrc
mv ~/.bashrc /tmp
SCRIPT_DIR=$( cd "$( dirname "$0" )" && pwd )
ln -s $SCRIPT_DIR/bash/bashrc ~/.bashrc

# add in bundles from bundle.txt
#mkdir -p ~/.vim/bundle
#bundles=$(cat ./bundles.txt)
#cd ~/.vim/bundle
#for repo in $bundles; do
#    git clone $repo
#done
